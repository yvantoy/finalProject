import { SafeAreaView } from 'react-native';
import Messenger from './Messenger';

const App = () => {
  return(
    <SafeAreaView style={{flex: 1}}>
      <Messenger/>
    </SafeAreaView>
  );
};

export default App;